// Password hash configuration template
// Copy this file to config.js and update the hash value
// Generate a new hash with: echo -n "your_password" | shasum -a 256 | cut -d' ' -f1
const CONFIG = {
    EXPECTED_HASH: "your_sha256_hash_here"
};
